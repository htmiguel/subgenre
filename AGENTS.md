# Local Agent Policy

Follow workspace policy from `~/projects/AGENTS.md`.

## Required defaults
- Keep this repo aligned with `~/projects/_shared/AGENT_RULES.md`.
- Use shared skills from `~/projects/_shared/skills/` before ad-hoc patterns.
- Keep project changes small, scoped, and private by default.
